import '../asset/scss/index.scss' /// Next permet d'incorporer les codes SCSS sans besoin de transiplateur

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}